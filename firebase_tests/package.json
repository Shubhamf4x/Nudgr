{
  "name": "nudgr-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore security rules tests for Nudgr (Firebase Emulator Suite)",
  "scripts": {
    "test": "mocha firestore.rules.test.js --timeout 20000"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^13.0.0",
    "mocha": "^10.4.0"
  }
}
